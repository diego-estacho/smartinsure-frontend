# components/

- `ui/` — básicos sem domínio (botão, modal, tabela), encapsulando o Vuetify (ADR-003).
- Pastas de domínio (ex.: componentes de uma jornada) entram **quando OPEN-01 fechar**
  (glossário + máquina de estados ratificados). Nada de termo de domínio antes disso.
